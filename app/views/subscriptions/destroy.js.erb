<%= render 'layouts/notifications' %>

(function() {
    <% unless @subscription.persisted? %>
    $('.subscribe-link').replaceWith('<%= j link_to 'Subscribe to the question',
     question_subscriptions_path(@subscription.question), method: :post, remote: true, class: 'subscribe-link' %>');
    <% end %>
})();
